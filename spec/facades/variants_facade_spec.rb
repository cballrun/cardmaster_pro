require 'rails_helper'

RSpec.describe VariantsFacade do

    # it 'returns an array of variants' do
    #     search_term = "Wartortle"
    #     variants_array = VariantsFacade.create_variants_by_search_term(search_term)
    #     first_variant = variants_array.first
       
    #     expect(variants_array).to be_a(Array)
    #     expect(variants_array.last).to be_a(Variant)
    #     expect(first_variant.name).to be_a(String)
    #     expect(first_variant.url).to be_a(String)
    #     expect(first_variant.tcgplayerid).to be_a(String) #only testing for attributes that will be present on every variant
    # end
end