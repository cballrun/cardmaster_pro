require "rails_helper"

RSpec.describe VariantsService do
    describe "initialization" do
        
        # it 'exists' do
        #     search_term = "Charmander"
        #     variant_service = VariantsService.new(search_term)

        #     expect(variant_service).to be_a(VariantsService)
        # end
    end

    describe "searching & navigation" do
    #     it "enters a given search term and submits it" do
    #         search_term = "Charmander"
    #         variant_service = VariantsService.new(search_term)

    #         expect(@driver.current_url).to not_equal("https://www.tcgplayer.com/")
    #     end
    end

    describe "scraping" do
    #     it "returns an array of CardVariant structures for all search results" do
    #         search_term = "Ivysaur"
    #         variant_service = VariantsService.new(search_term)
    #         variants = variant_service.scrape_variants
    #         first_variant = variants.first

    #         expect(variants).to be_a(Array)
    #         expect(first_variant.url).to be_a(String)
    #         expect(first_variant.name).to be_a(String)
    #         expect(first_variant.tcgplayerid.to_i).to be_a(Integer) #only testing for the 3 fields all variants have
    #     end
    end
end